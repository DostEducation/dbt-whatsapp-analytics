version: 2

models:
  
  - name: fct_contacts
    columns: 
      - name: contact_phone
        description: primary key for this table
        tests: 
          - not_null
          - unique
  - name: fct_messages
    columns: 
      - name: message_id
        description: primary key for this table
        tests: 
          - not_null
          - unique
  
  - name: fct_outputs
    columns: 
      - name: output_id
        description: primary key for this table
        tests: 
          - not_null
          - unique
  
  - name: fct_nodes
    columns: 
      - name: node_label
        description: primary key for this table
        tests: 
          - not_null
          - unique